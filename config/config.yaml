network:
  name: ViT
  pretrained: True
  # Specify a folder containing a pre-trained model to fine-tune. If training from scratch, pass None.
  checkpoints: 'checkpoints'

  projection_head:
    mlp_hidden_size: 512
    projection_size: 128

data_transforms:
  s: 1
  input_shape: (96,96,3)

trainer: #byol trainer
  batch_size: 128
  m: 0.996 # momentum update
  checkpoint_interval: 5000
  max_epochs: 50
  num_workers: 6

classifier_trainer:
  batch_size: 1024
  m: 0.996 # momentum update
  checkpoint_interval: 5000
  max_epochs: 40
  num_workers: 6

mode: 'train_classifier' #optuna/train_byol/train_classifier/test_classifier

optimizer:
  params: #byol_params 
    lr: 0.00001
    # momentum: 0.9
    weight_decay: 0.0 #0.0004
  classifier_params:
    lr: 0.0003
    # momentum: 0.9
    weight_decay: 0.0 #0.0004
